using Gtk 4.0;

template $PowerStatesList: Frame {
    hexpand: true;

    [label]
    Label {
        label: bind template.title;
        margin-start: 5;
        margin-end: 5;
    }

    ListBox states_listbox {
        selection-mode: browse;
    }
}
